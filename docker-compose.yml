services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8001

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    environment:
      - API_URL=http://localhost:8001
    depends_on:
      - backend
